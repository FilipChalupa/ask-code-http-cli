services:
  agent:
    build: .
    container_name: ask-code-agent
    env_file: .env
    environment:
      - QUESTION=${QUESTION:-}
    ports:
      - "3000:3000"
    volumes:
      - repo:/repo
    restart: unless-stopped

volumes:
  repo:
