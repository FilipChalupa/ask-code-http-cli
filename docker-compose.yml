services:
  agent:
    build: .
    container_name: ask-code-agent
    env_file: .env
    environment:
      - QUESTION=${QUESTION:-}
    ports:
      - "${HOST_PORT:-3000}:3000"
    volumes:
      - repo:/repo
      - sessions:/sessions
    restart: unless-stopped

volumes:
  repo:
  sessions:
